node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

.dev.vars
.wrangler/

# Internal product docs (never published — strategy, roadmap, competitive analysis)
docs/internal/

# Vendored at install time from pinned packages (see package.json "vendor")
static/vendor/

# Spike 023 (OCR): self-hosted model weights + ORT wasm, fetched not committed
# (see spike-ocr/README.md for the download commands)
static/models/
static/ort/
