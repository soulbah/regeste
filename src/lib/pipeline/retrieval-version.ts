/** Increment only when persisted retrieval views must be rebuilt from originals. */
// 28: normalize reverse-drawn text, preserve geometric vertical table headers
// and mixed rows, and OCR page-dominating rasters that retain scan margins.
export const RETRIEVAL_VERSION = 28;
