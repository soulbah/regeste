/** Increment only when persisted retrieval views must be rebuilt from originals. */
// 22: PDF pages whose ruling binds values to labels are rebuilt from the table
// reconstruction, and a scan carrying a worthless OCR layer is now routed to
// recognition. Both change what a page yields, so passages indexed before this
// still carry the old split — a price severed from the service it prices.
export const RETRIEVAL_VERSION = 25;
