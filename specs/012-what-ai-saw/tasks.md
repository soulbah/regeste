# Tasks 012 — What AI saw

- [x] 1. Schema v7 (message_excerpts snapshots) + worker insert/list + generation paths record excerpts (private/myai/assisted incl. excluded).
      Done when: `bun run check` green; rows appear after each answer kind.
- [x] 2. WAIS panel component + right-panel priority + meta-line click-through.
      Done when: Verification 1–3 pass in dev.
- [x] 3. Closest-sources on citation-less grounded answers.
      Done when: Verification 4 passes in dev.
- [x] 4. End-to-end verification + PROGRESS.md update.
      Done when: `bun run verify` exits 0.

Completion checklist (all required before the spec is closed):

- [x] All tasks checked with their Done-when verified
- [x] spec.md "Verification" section executed end-to-end
- [x] `bun run verify` passes
- [x] PROGRESS.md updated
